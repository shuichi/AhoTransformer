[project]
name = "ahotransformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "argparse",
    "torch",
    "numpy",
    "tensorboard",
]

[tool.uv.pip]
torch-backend = "auto"
